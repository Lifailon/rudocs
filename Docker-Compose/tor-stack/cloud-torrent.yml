services:
  cloud-torrent:
    image: jpillora/cloud-torrent
    container_name: cloud-torrent
    restart: unless-stopped
    ports:
      - "9088:3000"
    volumes:
      # Директория для хранения видео в хостовой системе (WSL) : контейнере
      - "/mnt/d/plex-content:/downloads"
