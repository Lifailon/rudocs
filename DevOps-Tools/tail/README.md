## Tail

Реализация утилиты командной строки `tail` на Go.

```Bash
# -p, --path   - путь к файлу
# -l, --lines  - количество последних строк для отображения
# -f, --follow - отслеживать изменения в реальном времени
go run main.go -p main.go -l 5 -f
```