apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vault

resources:
- agent-injector-rbac.yaml
- agent-injector-deployment.yaml
- agent-injector-service.yaml
- agent-injector-webhook.yaml

# SA для настройки подключения к Vault
- vault-checker-rbac.yaml
# Под для проверки подключения к Vault
# - vault-checker-deployment.yaml