apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dozzle-ingress
  namespace: monitoring
spec:
  ingressClassName: traefik
  rules:
  - host: dozzle.k8s.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dozzle-service
            port:
              number: 80