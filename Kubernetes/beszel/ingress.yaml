apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: beszel-ingress
  namespace: monitoring
spec:
  ingressClassName: traefik
  rules:
  - host: beszel.k8s.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: beszel-service
            port:
              number: 80