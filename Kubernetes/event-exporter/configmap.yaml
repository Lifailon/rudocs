apiVersion: v1
kind: ConfigMap
metadata:
  name: event-exporter-config
  namespace: monitoring
data:
  config.yaml: |
    route:
      routes:
        - match:
            - receiver: "dump"
              # Фильтрация по типу события, ресурса или namespace 
              # type: "Warning"
              # kind: "Deployment"
              # namespace: "dev"
    receivers:
      - name: "dump"
        # Перенаправлять в лог контейнера
        stdout: {}
      # - name: telegram
      #   webhook:
      #     url: "https://telegram.orghttps://api.telegram.org/bot<TOKEN>/sendMessage"
      #     headers:
      #       Content-Type: "application/json"
      #     body:
      #       chat_id: "<CHAT_ID>"
      #       text: "Object: {{ .InvolvedObject.Name }}\nReason: {{ .Reason }}\nMessage: {{ .Message }}"